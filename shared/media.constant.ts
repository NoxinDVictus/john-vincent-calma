const link = '/assets';

const images = `${link}/images`;
const logos = `${link}/logos`;

export const IMAGES = {
  ME: `${images}/me.jpeg`,
};

export const LOGOS = {
  VERCEL: `${logos}/vercel.png`,
};
