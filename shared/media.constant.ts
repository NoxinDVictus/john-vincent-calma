const link = '/assets';

const images = `${link}/images`;
const logos = `${link}/logos`;

export const IMAGES = {
  ME: `${images}/me.jpeg`,
};

export const LOGO = {
  VERCEL: `${logos}/vercel.png`,
  BLACK: `${logos}/logo-black.svg`,
  WHITE: `${logos}/logo-white.svg`,
};
